# Scenario blueprints

- **已落地**：`coordination_student_v1.json`（沟通协作包，学生向）。
- **占位扩展**：`verification_*`、`decision_*`、`risk_boundary_*` 将按同一 `ScenarioBlueprint` schema 追加，并在 `lib/scenario-v2/loader.ts` 中静态注册 import。
